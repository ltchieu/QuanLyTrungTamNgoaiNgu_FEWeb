export interface StudentInfoResponse {
  studentId: number;
  name: string;
  dateOfBirth: string; 
  gender: boolean;
  jobs: string;
  email: string;
  phoneNumber: string;
  address: string;
  image: string;
}
